{# Brutalist horizontal-marquee BACKGROUND layer — sits BELOW the scroll-track text.
   Specimen: brutalist-marquee-v1.svg.
   Layer 0: flat slab fill from genome.surface_hex.
   The PERIMETER and ACCENT BAR live in brutalist-overlay.j2 so they render
   ABOVE the scroll text — text scrolls UNDER them at the edges.
   v0.2.16: LIVE block, divider, and LIVE label deleted entirely.
#}
<rect width="{{ mw }}" height="{{ mh }}" fill="{{ surface_hex }}"/>
